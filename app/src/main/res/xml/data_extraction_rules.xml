<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12（API 31）及以上的备份 / 传输规则。

  cloud-backup   Google 云备份
  device-transfer 换机时的设备到设备直传

  两条都全量排除。理由同 backup_rules.xml：
  被备份走的数据不再受 signature 权限保护，等于给私密联系人开了后门。
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
        <exclude domain="root" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="database" path="." />
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="external" path="." />
        <exclude domain="root" path="." />
    </device-transfer>
</data-extraction-rules>
